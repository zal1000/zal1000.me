import colors from 'tailwindcss/colors'

module.exports = {
    theme: {
      extend: {
        colors: {
          'light-blue': colors.sky,
          cyan: colors.cyan,
        },
      },
    },
    variants: {},
    plugins: [],
  }